(function(){
 const body=document.body, nav=document.querySelector('.nav'), loader=document.querySelector('.loader'), transition=document.querySelector('#page-transition'), progress=document.querySelector('#progress');
 window.addEventListener('load',()=>setTimeout(()=>loader&&loader.classList.add('hide'),500));
 window.addEventListener('scroll',()=>{const y=scrollY; if(nav)nav.classList.toggle('scrolled',y>30); if(progress){const h=document.documentElement.scrollHeight-innerHeight;progress.style.width=(h?y/h*100:0)+'%'} document.querySelectorAll('.parallax').forEach(el=>{const r=el.getBoundingClientRect(), speed=parseFloat(el.dataset.speed||.12); if(r.bottom>0&&r.top<innerHeight) el.style.transform=`translate3d(0,${(innerHeight/2-r.top)*speed*-1}px,0)`});});
 const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('in');io.unobserve(e.target)}}),{threshold:.12}); document.querySelectorAll('.reveal').forEach(e=>io.observe(e));
 document.querySelectorAll('[data-count]').forEach(el=>{const target=parseFloat(el.dataset.count);const obs=new IntersectionObserver(es=>{if(!es[0].isIntersecting)return;let s=0,t=700,start=performance.now();function tick(now){const p=Math.min((now-start)/t,1),v=target*(1-Math.pow(1-p,3));el.textContent=(target%1?v.toFixed(1):Math.round(v)).toLocaleString();if(p<1)requestAnimationFrame(tick)}requestAnimationFrame(tick);obs.disconnect()},{threshold:.7});obs.observe(el)});
 const cursor=document.querySelector('.cursor');if(cursor&&matchMedia('(pointer:fine)').matches){let mx=innerWidth/2,my=innerHeight/2,cx=mx,cy=my;addEventListener('mousemove',e=>{mx=e.clientX;my=e.clientY});(function loop(){cx+=(mx-cx)*.18;cy+=(my-cy)*.18;cursor.style.left=cx+'px';cursor.style.top=cy+'px';requestAnimationFrame(loop)})();document.querySelectorAll('a,.btn,.audience-card,.coach-card,.card').forEach(el=>{el.addEventListener('mouseenter',()=>cursor.classList.add('big'));el.addEventListener('mouseleave',()=>cursor.classList.remove('big'))})}
 document.querySelectorAll('.magnetic').forEach(el=>{el.addEventListener('mousemove',e=>{const r=el.getBoundingClientRect(),x=e.clientX-r.left-r.width/2,y=e.clientY-r.top-r.height/2;el.style.transform=`translate(${x*.12}px,${y*.12}px)`});el.addEventListener('mouseleave',()=>el.style.transform='')});
 document.querySelectorAll('a[href$=".html"],a[href="/"]').forEach(a=>{const href=a.getAttribute('href');if(!href||href.startsWith('#')||href.startsWith('mailto:')||a.target==='_blank')return;a.addEventListener('click',e=>{e.preventDefault();transition.style.transform='scaleY(1)';transition.style.transition='transform .55s cubic-bezier(.77,0,.18,1)';setTimeout(()=>location.href=href,430)})});
 const journey=document.querySelector('.journey-track'), line=document.querySelector('.journey-line i');if(journey){addEventListener('scroll',()=>{const sec=journey.closest('section'),r=sec.getBoundingClientRect(),p=Math.max(0,Math.min(1,(innerHeight-r.top)/(innerHeight+r.height)));const max=journey.scrollWidth-innerWidth+80;journey.style.transform=`translateX(${-max*p}px)`;if(line)line.style.left=(p*100)+'%'});}
 document.querySelectorAll('[data-tilt]').forEach(card=>{card.addEventListener('mousemove',e=>{const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.transform=`perspective(900px) rotateX(${y*-3}deg) rotateY(${x*3}deg) translateY(-6px)`});card.addEventListener('mouseleave',()=>card.style.transform='')});
 const menu=document.querySelector('[data-menu]');if(menu){menu.addEventListener('click',()=>{const links=document.querySelector('.links');links.style.display=links.style.display==='flex'?'':'flex';links.style.position='absolute';links.style.top='70px';links.style.left='18px';links.style.right='18px';links.style.padding='22px';links.style.background='rgba(251,250,246,.97)';links.style.color='var(--ink)';links.style.flexDirection='column';links.style.alignItems='stretch';links.style.border='1px solid var(--line)';links.style.backdropFilter='blur(18px)'})}
 const form=document.querySelector('[data-contact-form]');if(form)form.addEventListener('submit',e=>{e.preventDefault();const d=new FormData(form);const subject=encodeURIComponent('FORE enquiry — '+d.get('audience'));const body=encodeURIComponent(`Name: ${d.get('name')}\nEmail: ${d.get('email')}\nOrganisation: ${d.get('org')||''}\n\n${d.get('message')||''}`);location.href=`mailto:hello@foresports.in?subject=${subject}&body=${body}`});
})();
